name: Build and push backend ASP image

on:
  push:
    branches: ['main']

jobs:
  build-and-push-image:
    uses: ./.github/workflows/build-and-push.yml
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    with:
      path: backend/asp
      tag: asp